// Placeholder smoke test — the app boots into a pairing/session flow that
// needs platform channels, so just verify the entry point builds a widget.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('trivial smoke', () {
    expect(1 + 1, 2);
  });
}
